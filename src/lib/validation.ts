type UnknownRecord = Record<string, unknown>; export function text(value: unknown, max = 200) { return typeof value === "string" ? value.trim().slice(0, max) : ""; } export function isEmail(value: string) { return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value); } export function asRecord(value: unknown): UnknownRecord | null { return value !== null && typeof value === "object" && !Array.isArray(value) ? value as UnknownRecord : null; }
export async function requestData(request: Request): Promise<UnknownRecord | null> { const contentType = request.headers.get("content-type") ?? ""; if (contentType.includes("application/json")) return asRecord(await request.json()); return asRecord(Object.fromEntries((await request.formData()).entries())); }
