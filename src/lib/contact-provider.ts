import "server-only"; import type { ContactRequest, QuoteRequest } from "@/lib/shopify/types";
export async function deliverInquiry(kind: "contact" | "quote", payload: ContactRequest | QuoteRequest) { const webhook = process.env.CONTACT_WEBHOOK_URL; if (!webhook) return { delivered: process.env.NODE_ENV !== "production", provider: "development-sink" }; const response = await fetch(webhook, { method: "POST", headers: { "Content-Type": "application/json", ...(process.env.CONTACT_WEBHOOK_TOKEN ? { Authorization: `Bearer ${process.env.CONTACT_WEBHOOK_TOKEN}` } : {}) }, body: JSON.stringify({ kind, payload }) }); if (!response.ok) throw new Error(`Contact provider rejected the request (${response.status}).`); return { delivered: true, provider: "webhook" }; }
