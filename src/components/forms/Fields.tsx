export function Field({ label, name, type = "text", required = false, defaultValue, readOnly = false }: { label: string; name: string; type?: string; required?: boolean; defaultValue?: string; readOnly?: boolean }) { return <label className="field"><span>{label}{required && " *"}</span><input name={name} type={type} required={required} defaultValue={defaultValue} readOnly={readOnly} /></label>; }
export function TextareaField({ label, name, required = false, placeholder }: { label: string; name: string; required?: boolean; placeholder?: string }) { return <label className="field field--full"><span>{label}{required && " *"}</span><textarea name={name} rows={5} required={required} placeholder={placeholder} /></label>; }
