import Image from "next/image"; import type { Product } from "@/lib/shopify/types";
export function ProductGallery({ product }: { product: Product }) { const images = product.images.length ? product.images : product.featuredImage ? [product.featuredImage] : []; return <div className="product-gallery">{images[0] ? <div className="product-gallery__main"><Image src={images[0].url} alt={images[0].altText || product.title} fill priority sizes="(max-width: 800px) 100vw, 50vw" style={{ objectPosition: images[0].focalPosition ?? "center" }} /></div> : <div className="product-gallery__empty">Chưa có hình ảnh sản phẩm</div>}</div>; }
