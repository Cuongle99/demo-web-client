import Link from "next/link"; import { CheckCircle, Phone } from "@phosphor-icons/react/dist/ssr"; import type { Product } from "@/lib/shopify/types"; import { siteConfig } from "@/config/site";
function price(product: Product) { const variant = product.variants[0]; if (!variant || Number(variant.price.amount) === 0) return "Liên hệ để nhận báo giá"; return new Intl.NumberFormat("vi-VN", { style: "currency", currency: variant.price.currencyCode, maximumFractionDigits: 0 }).format(Number(variant.price.amount)); }
export function ProductInfo({ product }: { product: Product }) { const variant = product.variants[0]; return <div className="product-info"><p className="eyebrow">{product.productType || "Thiết bị y tế"}</p><h1>{product.title}</h1><div className="product-info__meta"><span>SKU: <strong>{variant?.sku || "Đang cập nhật"}</strong></span><span>Thương hiệu: <strong>{product.vendor || "Toàn Tâm"}</strong></span></div><p className="product-info__price">{price(product)}</p><ul><li><CheckCircle weight="fill" />Cam kết hàng chính hãng</li><li><CheckCircle weight="fill" />Tư vấn cấu hình phù hợp nhu cầu</li><li><CheckCircle weight="fill" />Bảo hành và hỗ trợ kỹ thuật</li></ul><div className="product-info__actions"><Link className="button button--primary" href={`/request-quote?product=${encodeURIComponent(product.handle)}`}>Yêu cầu báo giá</Link><a className="button button--outline" href={siteConfig.phoneHref}><Phone weight="fill" /> {siteConfig.phone}</a></div></div>; }
