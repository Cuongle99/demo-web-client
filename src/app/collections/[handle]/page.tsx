import type { Metadata } from "next"; import Link from "next/link"; import { notFound } from "next/navigation"; import { Breadcrumb } from "@/components/ui/Breadcrumb"; import { ProductGrid } from "@/components/product/ProductGrid"; import { getCollection } from "@/lib/shopify/collections";
export async function generateMetadata({ params }: PageProps<"/collections/[handle]">): Promise<Metadata> { const { handle } = await params; const collection = await getCollection(handle); if (!collection) return { title: "Không tìm thấy danh mục" }; return { title: collection.seo.title || collection.title, description: collection.seo.description || collection.description, alternates: { canonical: `/collections/${handle}` } }; }
export default async function CollectionPage({ params, searchParams }: PageProps<"/collections/[handle]">) { const { handle } = await params; const { after } = await searchParams; const collection = await getCollection(handle, 24, typeof after === "string" ? after : undefined); if (!collection) notFound(); return <div className="inner-page container"><Breadcrumb items={[{ label: "Trang chủ", href: "/" }, { label: "Danh mục" }, { label: collection.title }]} /><header className="collection-header"><p className="eyebrow">Danh mục sản phẩm</p><h1>{collection.title}</h1><p>{collection.description}</p></header><ProductGrid products={collection.products} />{collection.pageInfo?.hasNextPage && collection.pageInfo.endCursor && <div className="pagination"><Link className="button button--outline" href={`/collections/${handle}?after=${encodeURIComponent(collection.pageInfo.endCursor)}`}>Xem thêm sản phẩm</Link></div>}</div>; }
